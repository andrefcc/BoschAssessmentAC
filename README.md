Run scripts from the repository root folder:
python Scripts/<script_name>.py

First, run Bronze scripts (with no specific order)
Second, run Silver scripts (with no specific order)
Thirdly, run Gold script

There already 3 folders (Bronze; Silver; Gold) with outputs from the runned scripts
